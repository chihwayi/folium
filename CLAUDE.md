# Folium

Shared standards for all agents (project structure, DB/UI/backend
conventions, git workflow, deployment) live in [`AGENTS.md`](./AGENTS.md) —
read that first, it applies to Claude too.

Sprint plan and ownership: [`docs/sprints/README.md`](./docs/sprints/README.md).
Claude owns Sprints 0, 1, 7 and reviews every Codex PR for the rest before
merge (correctness, security, adherence to `AGENTS.md`).
